import Icon from '../../anonym.png';

const ProfileProptypeDefault =  {
    testavatar: <img className="defaultIcon" src={Icon} alt="Icon" />,
    user : {
        avatar : <img className="defaultIcon" src={Icon} alt="Icon" />
    }
    
};

export default ProfileProptypeDefault;